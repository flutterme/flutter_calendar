name: mini_calendar
description: Date component developed with Flutter, plans to support display, swipe left and right, add date mark, radio, display week, etc.
version: 3.0.0
repository: https://github.com/collectFlutter/mini_calendar

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
